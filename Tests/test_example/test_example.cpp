// test_example.cpp : 此文件包含 "main" 函数。程序执行将在此处开始并结束。
//

#include <Windows.h>

int main()
{
    MessageBoxA(NULL, "Hello, World!1", "Message", MB_OK);
    MessageBoxA(NULL, "Hello, World!2", "Message", MB_OK);
    MessageBoxA(NULL, "Hello, World!3", "Message", MB_OK);
    return 0;
}
